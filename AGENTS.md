# P_MAIN_experiment

ユーザーには日本語で回答する。PicoはPlatformIO、SpresenseはSony公式Arduinoコア3.4.7。

- ピン変更は `docs/HARDWARE.md` と隣接AquaBeaconの設計・実配線で確認する。元のAquaBeaconを変更しない。
- GP7はSpresenseからのUART受信専用。旧EVENT_MARK出力コードを流用しない。
- GP16は本取得実験ではLow固定。TX送信は新たな実験指示があるまで追加しない。
- 生ログ・認証情報・Wi-Fiパスワードをコミットしない。
- `sync=1` は対応条件成立を表し、UTC精度の実測合格を意味しない。未検証事項を合格と記載しない。
- 変更に応じてC++共通テスト、Pythonログ検証テスト、両Pico構成、Spresenseビルドを行う。
- 実機書き込み前は対象COMポートと親機/子機の識別を確認する。自動的に最初のPicoへ書き込まない。
- Web原本は `web/index.html`。生成される `web_page.h` を直接編集しない。

## 実験・検証レポート（必須）

- 実験・動作確認・テストごとに、結果を報告する前に必ずレポートを作成する。再指示を待たない。
- 同じ目的の検証は1レポートにまとめ、途中のビルドや再テストを細切れにしない。別の実験目的・実機セッションは新しいレポートとする。
- `reports/YYYY-MM-DD_NNN_識別名/` にレポートごとに1フォルダを作る。本文は `README.md`、図は `figures/`、公開可能な根拠は `evidence/`。
- `docs/REPORT_TEMPLATE.md` を使い、目的・日時・対象commit、前提条件（ピン配置、接続機器一覧、未接続・未確認機器、給電、開発環境）、何をしたか、結果、未検証事項を簡潔に記載する。
- 実接続と想定構成、ビルド結果と実測、模擬データと実験データを明確に分ける。
- 比較値にはグラフ、接続関係には図など、理解に役立つ図を添える。単位・出典・条件を記載し、架空の測定結果を作らない。
- 根拠のログ・CI URL・測定値を残す。秘密情報や実測位置を含む生ログは `reports/raw/` に保存し、公開レポートには要約だけを載せる。
- `reports/README.md` に索引を追加し、最終回答からレポートへリンクする。
