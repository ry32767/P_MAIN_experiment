# Spresense公式ブートローダー導入の再開

| 項目 | 内容 |
|---|---|
| 日時 | 2026-09-20 02:07〜02:10 JST |
| 目的 | ユーザーの再開指示後、Spresenseの起動に必要な公式ファームを導入する |
| 開始HEAD | `9f46d38` |
| 対象 | Spresense COM7、Sony Arduino core 3.4.7 |
| 状態 | ライセンス確認・公式6パッケージ転送・ボード検証成功。取得アプリ起動確認済み |

## 前提条件

COM6とCOM7を再検出。COM6は親機、COM7は前回識別したCP210x経由のSpresense。両SDは挿入済みとのユーザー申告。IMU型番・SD型番/容量・基板版数は未確認。RX、水温、水圧、テザー、LCDは未接続との申告。

設計上の配線はSpresense拡張D02→J_SPR1.1→Pico GP6（物理9）、D01→J_SPR1.2→GP7（物理10）、GND→J_SPR1.3。3.3V、各100Ω、JP1=3.3V、JP10 pin1–2開放が前提。SpresenseのSDは拡張SDHCI、IMUはSPI5を想定。実配線の確認は未完了。詳細は[配線資料](../../docs/HARDWARE.md)。

## 実施内容と結果

1. 再開指示後、識別済みCOM7を指定し、Arduino CLIの公式`burn-bootloader`（sfutools、115200bps）を開始した。今回は実行前の権限拒否はなかった。
2. 公式手順のSubCore1〜5削除、sync、再起動処理のログを確認した。これは公式初期セットアップの処理であり、SDのフォーマットではない。
3. loader/GNSS/DNN/AES/sysutil/mp3decの公式パッケージを渡すインストーラーが起動した。02:09時点でflash_writerのプロセスは生存・応答あり。`stored_version.json`はまだ存在せず、ライセンス確認とパッケージ転送の完了は確認できていない。
4. ユーザーにライセンス画面の表示状況と、同意する場合の操作を案内した。画面自体をこちらで目視確認したとはしていない。
5. 前回の128B転送成功を再利用する`flash_spresense_128.py`を用意し、Python構文確認を実施した。今回はまだこの補助ツールで転送していない。ESPKは公式SDK配置と、公式インストーラーのバージョン確認ファイルが一致する場合に限定。各パッケージのボード側検証成功を確認し、不成立なら停止する。

## 残件

### 02:12以降の追試結果（同じ導入作業）

ユーザーのOK後、公式ログの`License agreement accepted.`とv3.4.3の確認ファイルを確認した。Windows版ツールが前回同様に1K転送開始付近で進まないため、中断して準備済み128B転送へ切り替えた。以下の全パッケージでボード側の`Package validation is OK.`と保存先を確認した。

| パッケージ | バイト数 | 再送回数 | 結果 |
|---|---:|---:|---|
| loader | 130080 | 0 | 検証・保存成功 |
| gnssfw | 454528 | 2 | 検証・保存成功 |
| dnnrt-mp | 109808 | 0 | 検証・保存成功 |
| AESM | 28944 | 0 | 検証・保存成功 |
| sysutil | 307168 | 1 | 検証・保存成功 |
| mp3dec | 48016 | 1 | 検証・保存成功 |

再起動後、updaterではなく取得プログラムのIMU/SDカウンターを受信した。これで起動の問題は解消した。転送中に再送はあったが、全件ボード側検証に成功しており、未検証転送を合格扱いしていない。詳細生ログは同じフォルダの`transfer128.log`。取得後の評価は[009](../2026-09-20_009_initial_sensors/README.md)に記録する。

ブートローダー導入・取得アプリ起動は完了。IMU精度、GPS測位、両SD保存、統合通信・時刻同期・精度については別途実測結果に基づき判定する。

生ログ：Git管理外`reports/raw/2026-09-20_008_spresense_boot/official.log`。インストーラーの生存は実行時点の観測であり、後から再開するときは同じセッションの状態を再確認する。
